// types.ts
export type LoginInfo = {
    id: string;
    name: string;
    bio: string;
  };

export type OrgInfo = {
  j_org_id: string;
  org_id : string;
  org_name: string;
  now_open: boolean;
  admin_flg: boolean;
}
  